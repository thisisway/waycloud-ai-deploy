{
  "description": "Dev-only: WHMCS's Capsule is Laravel's query builder, so the store is tested against the real thing on SQLite.",
  "require-dev": {
    "illuminate/database": "^9.52"
  },
  "config": { "platform": { "php": "8.1.0" } }
}
